// Using empty string so fetch calls are relative paths (e.g. "/login")
// The React dev server proxy in package.json forwards these to http://localhost:5000
const API_BASE_URL = "";

export default API_BASE_URL;
